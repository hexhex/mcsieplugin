b(a,1) :- a(bcd,2), a(e,3), not a(b,1).
a(bcd,2) :- not -a(bcd,2).
-a(bcd,2) :- not a(bcd,2).
a(e,3) :- not -a(e,3).
-a(e,3) :- not a(e,3).
a(b,1) :- not -a(b,1).
-a(b,1) :- not a(b,1).
:- not b(a,1).
a(bcd,2) :- &mcsequi[const]().
a(e,3) :- &mcsequi[const]().
